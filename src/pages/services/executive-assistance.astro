---
import Layout from '../../layouts/Layout.astro';
import ServiceDetail from '../../components/ServiceDetail.astro';
import { getService } from '../../data/site.ts';
const s = getService('executive');
---
<Layout title={`${s.name} — Mandrex VA Services`} description={s.lead}>
  <ServiceDetail s={s} />
</Layout>
